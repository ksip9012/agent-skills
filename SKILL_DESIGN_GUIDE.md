# スキル設計ガイドライン

新しい Skill（`SKILL.md`）を作成・改修する際に参照するチェックリスト。

出典: Claude Code開発者 thariq氏 (@trq212) の「Lessons from Building Claude Code」シリーズ、特に「How we use skills」。詳細な要約は `life` リポジトリの `gtd/004_document/it/AI.md`（および元プロジェクト `gtd/002_project/00000000_archive/20260517_thariq氏発言/memo.md`）を参照。

## 作成・改修時のチェックリスト

- [ ] **description はトリガー基準か**: 「ユーザー向け要約」ではなく「モデルがいつ起動すべきかの判断材料」になっているか。依頼されそうなフレーズを含めているか
- [ ] **自明な事項を書いていないか**: Claudeが元々知っていること（一般的なコーディング知識・コマンドの基本用法等）は書かず、「いつもと違う考え方」に導く情報だけに絞る
- [ ] **過度にinstructしていないか**: 逐一の手順ではなく「目標と制約」を示し、状況適応の余地を残す
- [ ] **段階的情報開示になっているか**: 参照情報が多い場合、フォルダ構成で分割し、必要な時だけ読ませる（1ファイルに全部詰め込まない）
- [ ] **Gotchasセクションがあるか**: 実際にClaudeが失敗したパターンを追記していく場所を用意する（無ければ空のプレースホルダーで良い）
- [ ] **ユーザー固有情報の扱いを考えたか**: 必要なら `config.json` 等に保存し、未設定時は `AskUserQuestion` で構造化して聞く設計にする
- [ ] **永続化すべきデータの置き場所を考えたか**: スキル削除時に消えると困るデータは `${CLAUDE_PLUGIN_DATA}` 等、スキル本体と分離した場所に置く

## 運用中に継続すること

- スキル使用中に実際の失敗（誤判定・想定外の入力・コマンド失敗など）に遭遇したら、その場でGotchasセクションに追記する
- 「起動されるべき時に起動されない／されなくていい時に起動される」と気づいたら description を見直す
